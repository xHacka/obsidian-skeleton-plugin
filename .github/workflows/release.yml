name: Release

on:
  push:
    tags:
      - "*"

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2

      - run: bun install

      - run: bun run build

      - run: zip ${{ github.event.repository.name }}.zip main.js manifest.json styles.css

      - uses: softprops/action-gh-release@v2
        with:
          draft: false
          files: |
            ${{ github.event.repository.name }}.zip
            main.js
            manifest.json
            styles.css
